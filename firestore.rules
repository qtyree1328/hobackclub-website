rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Form submissions - write only from Cloud Functions (admin SDK bypasses rules)
    // No client reads/writes allowed
    match /submissions/{document=**} {
      allow read, write: if false;
    }
  }
}
